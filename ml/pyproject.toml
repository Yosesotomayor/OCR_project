[project]
name = "leaselens-ml"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "uvicorn[standard]",
    "fastapi",
    "sentence-transformers",
    "pydantic-settings",
    "paddleocr",
    "paddlepaddle",
    "python-multipart"
]