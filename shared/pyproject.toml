[project]
name = "shared-schemas"
version = "0.1.0"
description = "Modelos Pydantic y utilerías compartidas"
dependencies = [
    "pydantic>=2.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"