[project]
name = "shared-schemas"
version = "0.1.0"
description = "Modelos Pydantic y utilerías compartidas"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0.0",
]

[build-system]
requires = ["hatchling", "pydantic>=2.0.0"]
build-backend = "hatchling.build"
